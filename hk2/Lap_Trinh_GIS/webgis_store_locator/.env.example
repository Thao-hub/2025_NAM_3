DJANGO_SECRET_KEY=change-me
DJANGO_DEBUG=true
DJANGO_ALLOWED_HOSTS=127.0.0.1,localhost

# Base email (fallback)
EMAIL_HOST=sandbox.smtp.mailtrap.io
EMAIL_PORT=2525
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
EMAIL_USE_TLS=true
EMAIL_USE_SSL=false
DEFAULT_FROM_EMAIL=Circle K & GS25 <no-reply@circlek-gs25.local>
FEEDBACK_NOTIFICATION_EMAIL=

# Optional: separate SMTP for customer order emails
ORDER_EMAIL_HOST=
ORDER_EMAIL_PORT=
ORDER_EMAIL_HOST_USER=
ORDER_EMAIL_HOST_PASSWORD=
ORDER_EMAIL_USE_TLS=
ORDER_EMAIL_USE_SSL=
TRASH_RETENTION_DAYS=15

DB_ENGINE=django.db.backends.postgresql
DB_NAME=webgis_db
DB_USER=postgres
DB_PASSWORD=
DB_HOST=localhost
DB_PORT=5432
